{
  "name": "typequest",
  "version": "1.0.0",
  "description": "Mini-jeu de role CLI en TypeScript strict",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/main.js"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
